"""
qgate.compat — Backward-compatible re-exports of legacy APIs.

This package preserves access to the original ``conditioning`` and
``monitors`` modules that existed before the v0.3 TrajectoryFilter
redesign.  New code should import from the top-level ``qgate`` package
or from the canonical module paths.
"""
